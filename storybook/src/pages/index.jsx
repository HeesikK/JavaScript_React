import CustomButton from "../components/custombutton";

const HomePage = () => {
  return (
    <CustomButton variant={"primary"} size={"medium"} shape={"round"}>
      추가
    </CustomButton>
  );
};

export default HomePage;
